#Gradle 8.9 cannot run on JDK 23+, so pin the daemon regardless of JAVA_HOME. 11 rather than 21 because
#validateJdkVersion wants the build JVM to be 11: jar compression output, and so plugins.json hashes, can vary by JDK.
toolchainVersion=11
