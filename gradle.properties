# Compile against the locally built ProjectX client (run `gradlew build` in ../client first).
# The upstream GitHub release URLs in project-config.gradle don't publish projectx-*.jar artifacts.
projectxClientDir=../client/runelite-client/build/libs
